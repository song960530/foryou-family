spring:
  datasource:
    url: jdbc:h2:tcp://localhost/~/foryoufamily
    username: foryoufamily
    password:
    driver-class-name: org.h2.Driver

  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        # show_sql: true
        format_sql: true
        # use_sql_comments: true
    open-in-view: false

logging.level:
  org.hibernate.SQL: debug
  # org.hibernate.type: trace